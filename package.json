{
  "name": "tree-sitter-matry",
  "version": "0.0.1",
  "description": "The tree-sitter grammar for the Matry language",
  "main": "bindings/node",
  "keywords": [
    "parsing",
    "incremental"
  ],
  "dependencies": {
    "nan": "^2.18.0"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.20.8"
  },
  "scripts": {
    "test": "tree-sitter test",
    "build": "tree-sitter generate && node-gyp build"
  }
}
